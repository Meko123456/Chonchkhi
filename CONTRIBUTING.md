# Contributing

- Branch off `main`, keep changes small and focused.
- Every change should build (`make check`) before you open a PR.
- Reference the issue you close with `Closes #N` in the PR/commit.
- Match the existing style (`.editorconfig`; run `make format` if Spotless is set up).
