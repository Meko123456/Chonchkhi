## What & why

<!-- Briefly describe the change and the motivation. -->

## Checklist
- [ ] Builds locally (`./gradlew :app:assembleDebug`)
- [ ] Unit tests pass (`./gradlew test`)
- [ ] Lint is clean (`./gradlew :app:lintDebug`)
- [ ] Linked the issue this closes (`Closes #N`)
